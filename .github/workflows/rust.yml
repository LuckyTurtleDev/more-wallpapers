name: Rust

on:
  push:
    branches: [main]
  pull_request:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@stable
      - name: Install xrandr
        run: sudo apt-get install -y libxrandr-dev libdbus-1-dev
      - run: cargo test --workspace --all-features
        env:
          RUST_BACKTRACE: 1

  windows:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@stable
        with:
          target: x86_64-pc-windows-gnu
      - run: cargo test --workspace --all-features --target x86_64-pc-windows-gnu
        env:
          RUST_BACKTRACE: 1

  macos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@stable
        with:
          target: x86_64-apple-darwin
      - run: cargo test --workspace --all-features --target x86_64-apple-darwin
        env:
          RUST_BACKTRACE: 1

  rustfmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check
